# DeepAgent 三阶段计划交接（2026-07-28）

> 用途：新会话继续实施三阶段交付计划。  
> 唯一事实源：GitHub 远程仓库。  
> 禁止依赖：旧容器、旧工作区、未推送的本地文件、上一会话的临时日志。

---

## 一、新会话直接使用的提示词

```text
@GitHub yuanchenglu/deepseekagent

请从 GitHub 远程仓库继续实施 DeepAgent 三阶段交付计划。不要依赖任何旧容器、旧工作区或未推送的本地文件；GitHub 远程仓库是唯一事实源。

## 一、首先按顺序完整读取

1. 仓库根目录 `AGENTS.md`
2. `docs/open-source-readiness/00-THREE-PHASE-DELIVERY-STATUS.md`
3. `docs/open-source-readiness/HANDOFF_2026-07-28.md`
4. `docs/open-source-readiness/07-PHASE-1-OPEN-SOURCE-CLI-ALPHA.md`
5. `docs/open-source-readiness/08-PHASE-2-WEBUI-STABLE-BETA.md`
6. `docs/open-source-readiness/09-PHASE-3-DUAL-MODE-ELECTRON.md`
7. `docs/open-source-readiness/10-ELECTRON-PREVIEW-STATUS.md`
8. `docs/HANDOFF-DEVELOPER.md`（只作为历史上下文；冲突处以 00 状态文档和 10 状态文档为准）
9. PR #6：`ci: establish automated Electron Preview DMG gate`
10. PR #6 的全部 changed files、最新 commits、Actions checks、Artifacts 和未解决 review threads

## 二、已知远程基线

- 开发分支：`develop`
- 发布分支：`master`
- 本交接创建时的 `develop` 基线：`96c1d47ea03d06bcb4c342bcc7f33d506dd2499b`
- PR #5 已合入 `develop`，Browser E2E 已真实通过
- 当前代码工作分支：`chatgpt/electron-preview-release-gate`
- 当前代码 PR：#6
- 本交接快照时 PR #6 Head：`4125b418772822e6bf8d6c3ea457b74bbb3cb22d`
- PR #6 已有 15 commits、9 changed files
- 当前文档分支：`chatgpt/update-three-phase-progress-2026-07-28`
- 当前所有工作均已上传 GitHub；不存在仅保存在旧容器中的代码

注意：上述 SHA 和 CI 状态是时间快照。接手后必须先重新读取远程最新状态，不得假定没有变化。

## 三、当前真实进展

### 第一阶段 CLI Alpha

- 主体代码约 90%–95% 完成
- 仍是 No-Go
- 核心阻断：外部凭据轮换、Git 历史清理、全 refs 重扫、干净 Mac 生命周期验收、真实模型任务、P0/P1 清零

### 第二阶段 WebUI Beta

- 主体代码约 90% 完成
- Ticket/Session 本地认证和 Browser E2E 已完成
- 仍是 No-Go
- 核心阻断：PR #6 CI 清理、Alpha→Beta 升级/回滚、CLI/WebUI/Hermes/OpenCode 共存和真实环境验收

### 第三阶段 Electron Preview

- 主体代码约 80% 完成
- 工作区锁已经修复为多读单写；旧交接文档中的“写+读未互斥”已过时
- 无签名 Apple Silicon DMG 构建与验证 workflow 已进入 PR #6
- 仍是 No-Go

## 四、PR #6 当前已知阻断

接手后先重新验证；若仍存在，则按以下顺序处理：

1. `WebUI i18n Coverage` 失败：缺少英文源 locale 的 3 个 key：
   - `changelog.initial`
   - `changelog.auth`
   - `changelog.electron`
2. 未解决 review：`.github/workflows/release-electron-preview.yml` 的 `cancel-in-progress: true` 会取消正式发布运行。应只允许 PR 验证被后续提交取消；`workflow_dispatch + publish=true` 必须串行且不能取消正在发布的运行。
3. Electron DMG workflow 必须完成以下真实门禁：
   - 全 refs Gitleaks
   - 版本契约
   - WebUI 安装、测试、构建、许可证审计
   - Electron Main 测试
   - 无签名 arm64 DMG 构建
   - Bundle ID、版本、架构、安装脚本和未签名状态验证
   - Manifest、SHA-256、artifact

不要因为 PR 显示 mergeable 就提前合并。只有 CI 全绿且 review thread 已处理，才允许 squash 合入 `develop`。

## 五、接下来必须执行

1. 重新读取 PR #6 最新 Head、CI、Artifacts 和 review threads。
2. 修复仍存在的 i18n 与 concurrency 问题。
3. 保持 PR #6 范围聚焦，不新增非发布必要功能。
4. 等待并核对全部检查真实通过。
5. 解决 review thread，更新 PR 描述和验收记录。
6. squash 合入 `develop`。
7. 更新：
   - `docs/open-source-readiness/00-THREE-PHASE-DELIVERY-STATUS.md`
   - `docs/open-source-readiness/10-ELECTRON-PREVIEW-STATUS.md`
   - 本交接文档或创建新的日期交接文档
8. 然后继续真实环境门禁：
   - CLI Alpha 凭据轮换与 Git 历史清理
   - 干净 Apple Silicon Mac 安装/升级/回滚/卸载
   - CLI/WebUI/Electron/Hermes/OpenCode 共存验证
   - Electron 多任务并发与故障恢复 E2E
   - 真实用户 Preview 测试和 P0/P1 清零

## 六、开发流程规范

适用于以下 5 个项目：

- deepseekagent
- deepcode
- deepseek_runtime
- llm-harness-agent
- oh-my-deepseek-harness

Remote：`https://github.com/yuanchenglu/<项目名>.git`

### 分支策略

开发分支为 `develop`，发布分支为 `master`。`develop` 分支已取消 PR 强制保护，可以直接推送。

### 执行优先级

#### 第一优先：走 PR 流程

1. 创建功能分支 → 提 Pull Request → 等待 CI 通过 → 合入 `develop`
2. 按项目 `docs/` 目录下的计划文档执行任务
3. 确保 PR 标题和描述清晰说明变更内容

#### 第二优先：异常处理与直推

如果 PR 流程持续出问题（如 CI 环境不可用、测试依赖无法安装、规则冲突等），按以下步骤处理：

1. 先分析原因：定位问题根因，确认是代码问题还是环境问题
2. 尝试修复：如果是代码问题（如缺少文件、配置错误），直接修
3. 解决不了 → 直推 `develop`：如果问题无法在当前环境解决，直接推送到 `develop` 分支，不阻塞流程

### 直推 develop 的纪律

#### 1. Commit 信息格式

Commit 信息必须包含「问题原因」和「技术债务」段落：

```text
<type>(<scope>): <变更说明>

## 问题原因
[写明为什么 PR 流程无法通过，根因是什么]

## 技术债务
[列出本次遗留的未解决问题、待办事项]
```

示例：

```text
feat(auth): add login ticket validation

## 问题原因
CI 环境的 Playwright 依赖版本与本地不一致，
E2E 测试在 CI 上无法运行。已手动验证本地通过。

## 技术债务
- Playwright 版本锁定需要统一管理
- E2E 测试在 CI 上需要单独排查
```

#### 2. 技术债务记录（二选一）

方式 A：记在 Commit 信息里（推荐）

- 在 commit 的「技术债务」段落用短横线列表写明
- 小路会根据 commit 信息判断是否要处理

方式 B：记在项目文档中

- deepseekagent → `docs/TECH_DEBT.md` 或 `docs/BUG_LIST.md`
- deepcode → `docs/BUG_LIST.md`
- deepseek_runtime → `docs/TECH_DEBT.md`
- oh-my-deepseek-harness → `docs/TECH_DEBT.md`
- llm-harness-agent → `TECH_DEBT.md`（根目录）
- 每条格式：`[日期] 描述 | 遗留原因 | 状态`

### 核心原则

- 能走 PR 就走 PR，直推是兜底方案不是默认方案
- 直推必须有交代——commit 信息要说清楚为什么和留下了什么
- 技术债务不怕有，怕没人知道——记下来就是解决的第一步

## 七、输出要求

每完成一个步骤，都要基于 GitHub 远程事实报告：

- 当前分支和 Head SHA
- PR / CI / review 状态
- 实际修改文件
- 已运行测试及结果
- 尚未验证内容
- 是否满足合入或发布门禁
- 已推送的远程分支/PR，确保新会话不依赖旧容器
```

---

## 二、交接完整性确认

- PR #6 的代码和诊断提交均已在远程分支。
- 本进度文档和交接文档位于远程文档分支。
- 新会话可仅凭 GitHub 远程仓库恢复全部必要上下文。
- 当前不存在必须从旧容器复制的文件。
