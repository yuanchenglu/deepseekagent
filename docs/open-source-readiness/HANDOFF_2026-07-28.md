# DeepAgent 三阶段计划交接（更新于 2026-07-29）

> 用途：新会话仅依赖 GitHub 远程继续处理工程计划或对外发帖。  
> 唯一事实源：GitHub 远程仓库。  
> 禁止依赖：旧容器、旧工作区、未推送文件、上一会话临时日志。  
> 完整执行计划：`三阶段执行计划PLAN.md` v2.7.0。  
> 当前事实状态：`00-THREE-PHASE-DELIVERY-STATUS.md`。  
> Electron 专项状态：`10-ELECTRON-PREVIEW-STATUS.md`。

---

## 一、新会话直接复制的交接提示词

```text
@GitHub yuanchenglu/deepseekagent

你现在接手 GitHub 仓库 `yuanchenglu/deepseekagent` 的 DeepAgent 三阶段产品与开源发布计划，并根据 GitHub 远程真实状态继续处理后续工程或对外发帖内容。

不要依赖任何旧容器、旧工作区、上一会话临时日志或未推送文件。GitHub 远程仓库是唯一事实源。开始前必须重新读取远程最新状态，不得只依据本提示词中的 SHA 快照。

# 一、首先读取和核对

请按顺序读取：

1. 仓库根目录 `AGENTS.md`
2. `docs/open-source-readiness/三阶段执行计划PLAN.md`
   - 当前版本应为 v2.7.0 或更新版本
3. `docs/open-source-readiness/00-THREE-PHASE-DELIVERY-STATUS.md`
4. `docs/open-source-readiness/HANDOFF_2026-07-28.md`
5. `docs/open-source-readiness/07-PHASE-1-OPEN-SOURCE-CLI-ALPHA.md`
6. `docs/open-source-readiness/08-PHASE-2-WEBUI-STABLE-BETA.md`
7. `docs/open-source-readiness/09-PHASE-3-DUAL-MODE-ELECTRON.md`
8. `docs/open-source-readiness/10-ELECTRON-PREVIEW-STATUS.md`
9. `docs/open-source-readiness/11-RUNTIME-TASK-WORKSPACE-LEASE-PROTOCOL.md`
10. `.github/workflows/release-electron-preview.yml`
11. `.github/workflows/webui-browser-e2e.yml`
12. `scripts/check-electron-preview-concurrency.py`
13. 最近 20 个 `develop` commits
14. 所有开放 Pull Request
15. 失败、排队或进行中的 GitHub Actions
16. 所有未解决 review threads
17. 是否有远程分支领先 `develop` 但尚未合入

开始工作前必须明确：

- `develop` 最新 Head
- `master` 最新 Head
- 当前开放 PR 和远程未合入分支
- 当前 CI 和 review 状态
- 文档记录是否符合代码现实
- 是否存在仅保存在当前容器中的工作；如有，必须先推到远程

# 二、已知交接快照

以下只是 2026-07-29 的快照，必须重新验证：

- `develop`：`e0f2f407daa6f273ee4c927934efc2e3b27293a0`
- `master`：`b3943ac43f0f0f6a1f86f5f2cb9a230527389d91`
- PR #15：Runtime Task / Workspace Lease 协议已合入
- PR #17 最终 Head：`aba94fab7b36f9bd140752c455acdd4838bd3835`
- PR #17 squash merge：`e0f2f407daa6f273ee4c927934efc2e3b27293a0`
- PR #17 已通过 Browser E2E、全 refs 密钥扫描、WebUI、许可证、Electron Main、无签名 DMG、Manifest、SHA-256 和 artifact
- PR #17 未解决 review thread 为 0
- 未创建 Tag、Release 或公开 Preview channel
- 文档同步分支：`chatgpt/sync-plan-handoff-v2-7`

# 三、当前三阶段真实进展

## CLI Alpha

- 工程实施约 90%–95%
- 发布结论：No-Go

主要未完成：

- 外部凭据轮换与旧凭据失效确认
- Git 历史有效秘密清理和全 refs 重扫
- 干净 Apple Silicon Mac 生命周期验收
- Hermes/OpenCode 共存
- 正式支持模型的真实 Agent 任务
- P0/P1 清零

## WebUI Beta

- 工程实施约 90%–92%
- 发布结论：No-Go

已完成 Browser E2E、本地 Ticket/Cookie 认证、loopback 默认监听和 WebUI 自动门禁。

主要未完成：

- Alpha → Beta 数据迁移和失败回滚
- 干净机生命周期
- CLI/WebUI/Hermes/用户 OpenCode 共存矩阵
- 官网和正式 Beta 渠道
- 外部测试周期与 P0/P1 清零

## Electron Preview

- 工程实施约 94%–95%
- 发布结论：No-Go

已完成：

- DeepAgent / DeepCode 双模式
- Workspace Lock 和 Renderer 所有权
- 正式发布 concurrency
- Main 权威 Runtime Lease 协议
- 真实 Main/Runtime task 与 PID 生命周期
- DeepAgent bridge PID 和 DeepCode 子进程 PID 绑定
- heartbeat、timeout、process-exit、runtime-crash、Main restart 恢复
- POSIX/Windows PID 指纹
- orphaned 失败关闭
- Supervisor Token 子进程隔离
- Browser E2E、WebUI、Electron Main 和无签名 DMG 全链路

尚未完成：

- 双 Runtime 同 Workspace 真实并发与故障 E2E
- 凭据和 Git 历史安全门禁
- 干净 Mac、Gatekeeper、升级和卸载
- CLI/Desktop/Hermes/OpenCode 共存
- 真实用户 Preview 与 P0/P1 清零

# 四、当前唯一第一优先工程任务

双 Runtime 同 Workspace 并发与故障 E2E。

不要并行启动凭据清理、干净机或其他后继工程任务，除非用户明确改变优先级。

必须验证：

1. reader-reader 并行
2. reader-writer 双向互斥
3. writer-writer 互斥
4. acquire 被拒绝时不得 spawn 或产生写副作用
5. 正常完成和取消后的租约终态
6. heartbeat timeout 不产生双写窗口
7. DeepAgent bridge crash 后 Runtime 级 orphaned
8. DeepCode 子进程 crash、PID 消失和 PID 重用
9. Main 重启后的可验证恢复和不可验证 orphaned
10. 一个 Runtime 崩溃不影响另一个 Runtime、窗口或无冲突 Workspace
11. 保留远程可审计日志、状态快照和故障证据
12. 最终 PR Head 必须通过 Browser E2E 和完整 Electron Preview workflow
13. 处理所有 actionable review 后才合入 `develop`
14. 合入后更新 PLAN、00 状态、10 Electron 状态和本交接

不得把协议测试、mock 集成测试或单 Runtime 测试描述为双 Runtime E2E。

# 五、如果本会话的目标是处理对外发帖

先以最新 GitHub 事实为依据整理帖子，不得根据旧对话夸大完成度。

可以表述：

- Runtime Lease 协议和真实 task/PID 生命周期已合入 `develop`
- Browser E2E、WebUI、Electron Main 和无签名 DMG 自动门禁已通过
- 项目进入发布收敛阶段

不得表述：

- Electron Preview 已公开发布
- DMG 已签名或公证
- CLI Alpha、WebUI Beta 或 Electron Preview 已达到 Go
- 双 Runtime 真实并发已经验证
- 整个仓库全部使用 MIT
- 凭据轮换、历史清理、干净机、共存或用户验收已经完成

每条帖子优先链接具体仓库文档或 PR。发布前再次核对链接、字符数、事实和当前远程状态。

# 六、开发流程规范

适用于以下 5 个项目：

- deepseekagent
- deepcode
- deepseek_runtime
- llm-harness-agent
- oh-my-deepseek-harness

Remote：`https://github.com/yuanchenglu/<项目名>.git`

## 分支策略

- 开发分支为 `develop`
- 发布分支为 `master`
- `develop` 分支已取消 PR 强制保护，可以直接推送

## 第一优先：走 PR 流程

1. 创建功能分支 → 提 Pull Request → 等待 CI 通过 → 合入 `develop`
2. 按项目 `docs/` 目录下的计划文档执行任务
3. 确保 PR 标题和描述清晰说明变更内容

PR 描述必须说明：问题根因、技术方案、修改范围、测试结果、未验证内容、技术债务。

## 第二优先：异常处理与直推

如果 PR 流程持续出问题，例如 CI 环境不可用、测试依赖无法安装、规则冲突：

1. 先分析原因：定位根因，确认是代码问题还是环境问题
2. 尝试修复：如果是代码问题，例如缺少文件、配置错误，直接修复
3. 解决不了则直推 `develop`：如果问题无法在当前环境解决，直接推送到 `develop`，不无限阻塞流程

## 直推 develop 的纪律

Commit 信息必须包含“问题原因”和“技术债务”段落：

<type>(<scope>): <变更说明>

## 问题原因
[写明为什么 PR 流程无法通过，根因是什么]

## 技术债务
- [列出本次遗留的未解决问题、待办事项]

示例：

feat(auth): add login ticket validation

## 问题原因
CI 环境的 Playwright 依赖版本与本地不一致，E2E 测试在 CI 上无法运行。已手动验证本地通过。

## 技术债务
- Playwright 版本锁定需要统一管理
- E2E 测试在 CI 上需要单独排查

## 技术债务记录（二选一）

方式 A：记在 Commit 信息里，推荐。

方式 B：记在项目文档中：

- deepseekagent → `docs/TECH_DEBT.md` 或 `docs/BUG_LIST.md`
- deepcode → `docs/BUG_LIST.md`
- deepseek_runtime → `docs/TECH_DEBT.md`
- oh-my-deepseek-harness → `docs/TECH_DEBT.md`
- llm-harness-agent → 根目录 `TECH_DEBT.md`

每条格式：`[日期] 描述 | 遗留原因 | 状态`

## 核心原则

- 能走 PR 就走 PR，直推是兜底方案，不是默认方案
- 直推必须有交代，Commit 信息要说清楚为什么和留下了什么
- 技术债务必须可追踪
- 会话结束前，所有关键代码、文档、补丁和诊断结论必须上传 GitHub 远程
- 不得依赖旧容器、旧工作区或未推送文件

# 七、执行与交付要求

- 每次只处理一个可独立验收的工作单元
- 不创建未经授权的 Tag、Release 或公开渠道
- 不把无签名 DMG 描述为 Stable
- 每次工程合入后同步计划和交接文档
- 会话结束时明确列出：远程分支、Commit、PR、CI、未合入内容和下一唯一任务
```

---

## 二、当前远程保存状态

本交接更新完成后，关键成果应存在于：

- 代码：`develop@e0f2f407daa6f273ee4c927934efc2e3b27293a0`
- 文档：`chatgpt/sync-plan-handoff-v2-7`

即使文档 PR 尚未合入，新会话也可以直接从远程分支读取，不依赖当前容器。

---

## 三、发布纪律

当前所有阶段仍为 **No-Go**。不得创建 Tag、Release、公开 Preview channel，或把工程候选制品描述为已发布产品。
