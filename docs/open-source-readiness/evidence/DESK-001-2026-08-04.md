# DESK-001 证据：干净 Apple Silicon Mac 验证 unsigned DMG 下载与 checksum

## Work ID: DESK-001
## 结论: PASSED
## 执行时间（UTC）: 2026-08-04T15:00Z ~ 2026-08-04T15:10Z
## 执行人/AI: local-ai
## 复核人: 小路
## 仓库: yuanchenglu/deepseekagent
## develop 基线 SHA: fa0162b8ae11bc169fd5c291639363bcaabc7035
## 功能分支: 无（纯验证任务，无代码改动）
## 最终 PR Head SHA: N/A
## 合并 SHA: N/A
## 操作系统/芯片: macOS 26.5.1 (Build 25F80) / Apple Silicon arm64
## 工具版本: gh CLI, curl, shasum (SHA-256), hdiutil

---

## 一、输入来源

- CI workflow: `release-electron-preview.yml`（workflow_dispatch）
- 触发: `gh workflow run release-electron-preview.yml --ref develop -f version=0.9.0-preview.1 -f publish=false`
- GitHub Actions run: **30877816349**
- develop HEAD: `fa0162b8ae11bc169fd5c291639363bcaabc7035`
- 构建结果: **success**（4m54s，build-unsigned job 全部通过）
- publish=false → 未触发 publish job（仅验证构建）

## 二、执行命令（不含 Secret）

```bash
# 触发 CI 构建 unsigned DMG（publish=false）
gh workflow run release-electron-preview.yml --repo yuanchenglu/deepseekagent \
  --ref develop -f version=0.9.0-preview.1 -f publish=false

# 下载 electron-preview-release artifact (id=8880229974, 118MB)
# 网络慢，用 curl 断点续传 --retry-all-errors 完成
curl -sL -C - -H "Authorization: Bearer $TOKEN" \
  -H "Accept: application/vnd.github+json" \
  -o artifact/release.zip --retry 30 --retry-all-errors \
  https://api.github.com/repos/yuanchenglu/deepseekagent/actions/artifacts/8880229974/zip

# 解压
unzip -q release.zip -d extracted

# 独立计算 DMG 的 SHA-256
shasum -a 256 DeepAgent-0.9.0-preview.1-arm64.dmg

# 与 .sha256 文件比对
shasum -a 256 -c DeepAgent-0.9.0-preview.1-arm64.dmg.sha256

# DMG 完整性
hdiutil verify DeepAgent-0.9.0-preview.1-arm64.dmg

# 挂载并验证 app bundle
hdiutil attach -nobrowse -readonly -mountpoint "$MOUNT" DeepAgent-0.9.0-preview.1-arm64.dmg
/usr/libexec/PlistBuddy -c 'Print :CFBundleIdentifier' .../Info.plist
/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' .../Info.plist
file .../Contents/MacOS/DeepAgent
codesign --verify --deep --strict .../DeepAgent.app
```

## 三、关键断言（全部通过）

### 3.1 SHA-256 三方比对一致 ✅

| 来源 | SHA-256 |
|------|---------|
| **独立计算**（本机 shasum） | `957eb67eb208f8331df9b86ce8d15d03dbf17bea060f6fe9a719aec7e530c0ac` |
| **.sha256 文件** | `957eb67eb208f8331df9b86ce8d15d03dbf17bea060f6fe9a719aec7e530c0ac` |
| **manifest sha256** | `957eb67eb208f8331df9b86ce8d15d03dbf17bea060f6fe9a719aec7e530c0ac` |

`shasum -a 256 -c` → **OK**

### 3.2 artifact 大小一致 ✅

- 实际 DMG 大小: `118523936` 字节
- manifest 声明 size: `118523936` 字节
- 一致

### 3.3 DMG 完整性 ✅

- `hdiutil verify` → **checksum of "DeepAgent-0.9.0-preview.1-arm64.dmg" is VALID**
- 文件类型: zlib compressed data（有效 DMG）

### 3.4 app bundle 结构 ✅

- **Bundle ID**: `org.starseas.deepagent`
- **版本**: `0.9.0-preview.1`
- **二进制**: `Mach-O 64-bit executable arm64`（Apple Silicon）
- **安装脚本**: `Install DeepAgent.command` + `post-install.sh` 均存在且可执行
- **Applications 链接**: 存在

### 3.5 unsigned 确认 ✅

- `codesign --verify --deep --strict` 失败（`code has no resources but signature indicates they must be present`）
- 确认 DMG **未签名**，符合 Preview 预期（unsigned, notarized=no, gatekeeper_approval_required=true）

### 3.6 manifest 元数据 ✅

```json
{
  "product": "deepagent-desktop",
  "channel": "preview",
  "version": "0.9.0-preview.1",
  "runtime_version": "0.9.0-beta.1",
  "platform": {"os": "darwin", "arch": "arm64"},
  "distribution": {"signed": false, "notarized": false, "gatekeeper_approval_required": true}
}
```

## 四、原始本地证据位置

- artifact zip: `/tmp/desk001/artifact/release.zip`
- 解压目录: `/tmp/desk001/artifact/extracted/`
- 下载脚本: `/tmp/desk001/resume_dl.py`

## 五、P0/P1

- 无开放 P0/P1

## 六、回滚方案

- 本任务为纯验证，无代码/发布改动，无需回滚

## 七、Secret 脱敏检查

- Token 仅通过环境变量 `$TOKEN` 引用，未写入任何文件
- 证据文件不含任何 Secret 值

## 八、未验证内容

- DMG 实际启动/运行（属 DESK-002 及后续任务）
- 本机网络对 GitHub 下载慢（约 40KB/s），需断点续传完成，非任务阻塞

## 九、下一 Work ID: DESK-002

DESK-002：验证 Gatekeeper 手工批准路径。因为 Preview 无签名、未公证，需验证普通双击的真实 Gatekeeper 行为 + 右键 Open 路径，并确认限制说明在 Release Notes / 下载页 / 应用内一致。