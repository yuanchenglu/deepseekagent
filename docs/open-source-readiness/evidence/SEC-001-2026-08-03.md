## Work ID: SEC-001
## 结论: PASSED
## 执行时间（UTC）: 2026-08-03T06:05Z
## 执行人/AI: local-ai (Hermes Agent)
## 仓库: yuanchenglu/deepseekagent
## develop 基线 SHA: bada40b21493882db72ed690cf5b57abf896921d

## 凭据盘点结果

### 1. GitHub Repository Secrets（3 个，全部需轮换）

| Secret 名称 | 用途 | 创建时间(UTC) | 轮换状态 |
|---|---|---|---|
| CF_ACCOUNT_ID | Cloudflare 账户 ID（R2 endpoint + Pages） | 2026-07-02T06:41:44Z | 需轮换 |
| CF_R2_ACCESS_KEY_ID | R2 S3 兼容 API Access Key | 2026-07-02T06:41:45Z | 需轮换 |
| CF_R2_SECRET_ACCESS_KEY | R2 S3 兼容 API Secret Key | 2026-07-02T06:41:47Z | 需轮换 |

注意：CI workflow 还引用了 `CLOUDFLARE_API_TOKEN`（用于 Pages 部署），但 GitHub Secrets 中不存在。
这意味着该 workflow 步骤会在真实发布时失败，需要在 SEC-002 时一并创建此 Secret。

### 2. GitHub Organization Secrets
无（账户为个人账户 yuanchenglu，无 Organization）。

### 3. GitHub Environment Secrets
无 Environments 配置。

### 4. Dependabot Secrets
无。

### 5. CI Workflow 中引用的所有 Secret

| Workflow 文件 | 引用的 Secret |
|---|---|
| release.yml | CF_ACCOUNT_ID, CF_R2_ACCESS_KEY_ID, CF_R2_SECRET_ACCESS_KEY, CLOUDFLARE_API_TOKEN |
| release-electron-preview.yml | CF_ACCOUNT_ID, CF_R2_ACCESS_KEY_ID, CF_R2_SECRET_ACCESS_KEY |
| release-webui-beta.yml | CF_ACCOUNT_ID, CF_R2_ACCESS_KEY_ID, CF_R2_SECRET_ACCESS_KEY |

### 6. 代码环境变量（非发布凭据，不纳入轮换范围）

代码中引用了 100+ 个环境变量（DEEPSEEK_API_KEY, OPENAI_API_KEY, ANTHROPIC_API_KEY 等），
这些是 Hermes Agent 运行时用户自配的模型 Provider 凭据，不属于发布管线凭据。
Runbook 规定的轮换范围是"发布管线凭据"，不是用户运行时凭据。

### 7. Git 历史扫描

- feishu-bridge.env：占位符值（`cli_xxxxxxxxxxxxxxxx`、`your-app-secret`），无真实凭据
- runtime.env：占位符值（`sk-...`），无真实凭据
- 两个文件由 commit f745abb944 引入（内置 skill 打包），从未包含真实值
- Git 历史中未发现 AWS Access Key ID 格式（AKIA*）的真实值
- Git 历史中未发现硬编码的 R2/CF Secret 值

### 8. 外部平台凭据（需 Owner 确认）

| 平台 | 凭据 | 当前状态 | 轮换要求 |
|---|---|---|---|
| Cloudflare R2 | CF_ACCOUNT_ID + Access Key + Secret Key | 2026-07-02 创建，从未轮换 | 需创建新 R2 Token，限制到 deepagent-releases bucket |
| Cloudflare Pages | CLOUDFLARE_API_TOKEN | GitHub Secrets 中缺失 | 需创建新 CF API Token |
| GitHub | GITHUB_TOKEN | 自动生成，无需轮换 | 不适用 |

### 9. R2 Bucket 公开状态

- Bucket 名称：`deepagent-releases`
- 公开域名：`https://deepseekagent.starseas.org`
- 五个公开渠道（alpha/beta/stable/preview/stable）：全部 HTTP 404（未发布任何制品）
- 根路径 HTTP 200（域名可达，R2 已配置公开访问）

## 轮换计划

SEC-002 需要 Owner 创建以下最小权限凭据：
1. 新 R2 Access Key（只限 `deepagent-releases` bucket 读写）
2. 新 Cloudflare API Token（只限 Pages 部署权限）
3. 更新 GitHub Repository Secrets（CF_ACCOUNT_ID 可保持不变，轮换 Access Key 和 Secret Key）

## PASSED 条件验证
- [x] 所有已知服务均有条目
- [x] 每个条目明确"需轮换/不需轮换"
- [x] 无"无法确认"项
- [x] 清单完整性已验证（CI workflow + GitHub API + 代码扫描 + Git 历史）

## Secret 脱敏检查
通过（无 Secret 值出现在本报告中）

## 下一 Work ID: SEC-002
