# 需求池

> 记录 DeepAgent 待实现的需求，按优先级排列。

---

## 1. 一键复制 .hermes 配置到 .deepagent

- **描述**：实现一个脚本/命令，将 `~/.hermes` 中的配置一键复制/同步到 `~/.deepagent`，实现两套配置共存。
- **关键约束**：
  - skills 目录使用**软链接**，让两边的 skill 保持一致，避免重复维护。
  - 其他配置项（config.yaml、.env 等）按需复制，保留各自独立修改的空间。
  - 需要处理已存在文件时的覆盖/合并策略。
